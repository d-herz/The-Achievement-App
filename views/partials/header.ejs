<header>
    <% if (user) { %>
        <a href=<%=`/steam/${user.steamID}` %>><h1> <span class="logo1">Achievement</span><span class="logo2">App</span></h1></a>
    <% } else { %>
        <h1> <span class="logo1">Achievement</span><span class="logo2">App</span></h1>
    <% } %>

    <h2>Powered by Steam<sup>&reg;</sup></h2>
    <nav class="dashboard-nav">
        <% if (user) { %>
            <a href="/logout" class="btn logout">Logout</a>
        <% } %>
    </nav>
</header>